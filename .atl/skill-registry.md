# Skill Registry — conpas-forge

> Auto-generated by sdd-init. Do not edit manually — run `/skill-registry` to update.

## User Skills

| Skill | Trigger |
|-------|---------|
| `branch-pr` | When creating a pull request, opening a PR, or preparing changes for review |
| `engram-memory` | Always-active Engram persistent memory protocol for save/search/session-close behavior |
| `go-testing` | When writing Go tests, using teatest, or adding test coverage |
| `issue-creation` | When creating a GitHub issue, reporting a bug, or requesting a feature |
| `jedi-backend-erp-architecture-master` | When user needs Jedi/Yoda persona as ERP backend architect |
| `judgment-day` | When user says "judgment day", "judgment-day", "review adversarial", "dual review" |
| `skill-creator` | When user asks to create a new skill or document patterns for AI |
| `skill-registry` | When user says "update skills", "skill registry", "actualizar skills" |
| `zoho-deluge` | When working with Zoho Deluge scripts, .dg files, Creator functions, CRM workflows |

## SDD Skills (Orchestrator-managed)

`sdd-explore`, `sdd-propose`, `sdd-spec`, `sdd-design`, `sdd-tasks`, `sdd-apply`, `sdd-verify`, `sdd-archive`, `sdd-init`, `sdd-onboard`

## Project Conventions

No project-level CLAUDE.md or agents.md detected (new project).

## Compact Rules

### go-testing
- Use `go test ./...` for all tests
- Table-driven tests preferred
- Use `t.Run` for subtests
- No global state in tests

### zoho-deluge
- Always check statement execution limits (default: 5,000 per execution)
- Prefer bulk operations: `zoho.crm.bulkInsert`, `zoho.crm.bulkUpdate`
- NEVER place API calls (`invokeurl`, `zoho.crm.*`) inside `for each` loops
- Use Maps for O(1) lookups instead of nested loops
- Fetch all data before processing — single API call at top of function

### branch-pr
- Issue-first: create issue before PR
- Conventional commits: feat/fix/chore/refactor/docs
- One concern per PR

### judgment-day
- Trigger: explicit user request only
- Launches two independent blind judge sub-agents simultaneously
- Synthesizes findings, applies fixes, re-judges until both pass
