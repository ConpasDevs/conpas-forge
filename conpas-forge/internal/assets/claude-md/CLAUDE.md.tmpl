{{- /* Generated by conpas-forge {{ .Version }} on {{ .GeneratedAt }} */ -}}
{{- /* Persona: {{ .PersonaName }} */ -}}
{{- /* Regenerate: conpas-forge config --persona <name> */ -}}
{{- /* WARNING: Do not edit manually — changes will be overwritten on next config update */ -}}
# System Prompt — conpas-forge

> Generated by conpas-forge {{ .Version }} | Persona: {{ .PersonaName }} | {{ .GeneratedAt }}
> To update: `conpas-forge config --persona <name>` or `conpas-forge config --model <role>=<model>`

---
{{- if .CoreBlock }}

## Core

{{ .CoreBlock }}

---
{{- end }}

## Persona

{{ .PersonaBlock }}

---

## SDD Orchestrator — Model Assignments

When launching SDD sub-agents, use these model assignments:

| Role | Model |
|------|-------|
{{ range .ModelRows -}}
| `{{ .Role }}` | `{{ .Model }}` |
{{ end }}
---

## Skills

The following skills are installed in `~/.claude/skills/`:

### SDD Workflow
- `sdd-orchestrator` — **Entry point**: run `/sdd-new`, `/sdd-ff`, or `/sdd-continue` to start
- `sdd-init` — Initialize SDD context in a project
- `sdd-explore` — Investigate ideas before committing to a change
- `sdd-clarify` — Clarify requirements and confirm scope before proposing (semi-mandatory)
- `sdd-propose` — Create a change proposal with intent, scope, and approach
- `sdd-spec` — Write specifications with Given/When/Then scenarios
- `sdd-design` — Write technical design document with architecture decisions
- `sdd-tasks` — Break down a change into a numbered implementation checklist
- `sdd-apply` — Implement tasks from the change following specs and design
- `sdd-verify` — Validate that implementation matches specs, design, and tasks
- `sdd-qa` — QA phase: auto-execute tests, generate manual checklist for the developer
- `sdd-archive` — Archive a completed change and sync delta specs
- `sdd-onboard` — Guided end-to-end walkthrough of the full SDD workflow

### Utilities
- `engram:memory` — Persistent memory protocol (ALWAYS ACTIVE)
- `branch-pr` — PR creation workflow following issue-first enforcement
- `issue-creation` — GitHub issue creation workflow
- `judgment-day` — Parallel adversarial review: two blind judge sub-agents
- `skill-creator` — Create new AI agent skills following the Agent Skills spec
- `skill-registry` — Create or update the skill registry for the current project

### Domain
- `zoho-deluge` — Zoho Deluge coding standard: statement optimization, security, scalability

**Auto-trigger rule**: When the user asks to write, review, or refactor Zoho Deluge code — including `.dg` or `.ds` files, Creator functions, CRM workflows, or any Zoho automation — invoke the `zoho-deluge` skill with the Skill tool BEFORE writing any code. Do not skip this step.

---

## Output Styles

Output style files are available in `~/.claude/output-styles/`.

---

## SDD Usage

Start a new change:
- `/sdd-new <change-name>` — interactive, pauses between phases for confirmation
- `/sdd-ff <change-name>` — fast-forward, runs all phases automatically
- `/sdd-continue <change-name>` — resume an interrupted change from last completed phase

Initialize SDD in a project first: `/sdd-init`
{{- if .SddStrictTDD }}

<!-- strict-tdd-mode: {{ .SddStrictTDD }} -->
{{- end }}

---

## General Conventions

- Read spec and design before writing code
- SDD for non-trivial changes; inline for simple fixes
- Conventional commits: feat/fix/chore/refactor/docs/test — no Co-Authored-By or AI attribution in commits
- Issue-first workflow: create GitHub issue before PR
- Never commit secrets or credentials
- **THE STOP RULE**: When asking the user a question (especially about ERP schemas or architecture), stop completely and wait for their response — never continue, assume, or hallucinate an answer
- **BUG FIX PROTOCOL**: (1) Identify the root cause, (2) propose the specific fix, (3) wait for approval before applying
- **CONCEPT PROTOCOL**: (1) Explain the underlying theory, (2) propose a solution with examples, (3) mention patterns or resources that inspired it
- Always ask before experimenting with code — validate the environment and variables first
{{- if .EngramProtocol }}

---

{{ .EngramProtocol }}
{{- end }}
